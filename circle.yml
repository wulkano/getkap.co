test:
  override:
    - echo "test"

deployment:
  production:
    branch: master
    commands:
      - sudo rsync -avz -e --delete --rsh="ssh -o BatchMode=yes -p 22 -i" . circleci@getkap.co:/var/www/getkap.co/html/
