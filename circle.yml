test:
  override:
    - echo "test"

deployment:
  production:
    branch: master
    commands:
      - rsync -avz -e -O --delete --rsh="ssh -o BatchMode=yes -p 22" . circleci@getkap.co:/var/www/getkap.co/html/
